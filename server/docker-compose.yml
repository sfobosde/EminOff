version: "3.9"

services:

  image-server:

    build: .

    container_name: image-server

    restart: unless-stopped


    env_file:
      - .env


    ports:
      - "${PORT}:8000"


    volumes:
      - ./uploads:/data/uploads
      - ./events:/data

